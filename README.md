# Constructor
I use constructor in this program.
<pre>
  By using constructor we build a simple banking system
  1. In BankAccount class I initialize all the variables. IN this class we do:-
   a. we construct Parameterised constructor to initialise all three members.
   b. I make deposit function to deposit money in the bank account.
   c. I make withdraw function to withdraw money from the bank account if sufficient funds are available.
   d. And displayInfo function to display all the information like:- Account Holder, Account Number, Balance.
   e. And make getters and setters.
</pre>
